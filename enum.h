#pragma once

enum Genre {
    fantasy,
    scienceFiction,
    horror,
    crime,
    romance
};

enum CoverType {
    hardCover,
    paperBack
};

enum Format {
    PDF,
    EPUB
};

enum SearchCategory {
    ID,
    TITLE,
    AUTHOR,
    PRICE_RANGE
};
